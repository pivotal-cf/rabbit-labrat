<!doctype html>
<html>
  <head>
    <title>Environment information</title>
  </head>

  <body>
    <header>
      <ul>
        <li><a href="/services/rabbitmq/protocols/all">Check All Protocols</a></li>
        <li><a href="/services/rabbitmq/protocols/amqp091">Check AMQP 0-9-1 (all nodes)</a></li>
      </ul>
    </header>

    <article>
      <table>
        <% ENV.each do |k, v| %>
          <tr>
            <td><%= k %></td>
            <td><%= v.inspect %></td>
          </tr>
        <% end %>
      </table>
    </article>
    <article>
      <p>
        Parsed VCAP_SERVICES: <%= MultiJson.load(ENV.fetch("VCAP_SERVICES", "{}")).inspect %>
      </p>
    </article>
  </body>
</html>
