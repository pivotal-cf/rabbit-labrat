<!doctype html>
<html>
  <head>
    <title>
      Environment information
    </title>
  </head>

  <body>
    <article>
      <table>
        <% ENV.each do |k, v| %>
          <tr>
            <td><%= k %></td>
            <td><%= v.inspect %></td>
          </tr>
        <% end %>
      </table>
    </article>
  </body>
</html>
